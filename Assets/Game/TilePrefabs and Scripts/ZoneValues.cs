using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//values used to set information when coming into the new zone
public class ZoneValues : MonoBehaviour
{
    public Vector2 cameraMin;               //the minimum values the camera can move to
    public Vector2 cameraMax;               //the maximum values the camera can move to
    public string zoneName;                 //the name of the new zone
}
